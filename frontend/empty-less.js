// Stub used by next.config.js to replace `.less` imports coming from node_modules.
// `@antv/l7-component` ships raw `.less` files that Next.js does not compile;
// our flow-graph usage doesn't need those styles, so imports resolve here.
module.exports = {};
